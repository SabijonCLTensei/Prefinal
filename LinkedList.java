/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package sabijonqueuelinkedlist;

/**
 *
 * @author User
 */
public class LinkedList {
    public int next;
    public int prev;

    public LinkedList(int next, int prev) {
        this.next = next;
        this.prev = prev;
    }

    
    public Node enqueue(Object o){
        Node curr = data Node();
        curr = ;
        if (curr == ){
        
        }
    }
    public void dequeue(){
        int curr;
        curr = front;
        if (curr == null){
            
        }
    }
    public void peek(){
        front = curr;
        if ( == null){
        System.out.println("the queue empty");
        }
    }
    public String showContents(){
        return null;
    }
    
}
